using UnityEngine;
using System.Collections;
[RequireComponent(typeof (ParticlesSimpleBase))]

public class ParticlePropertyFade : MonoBehaviour {
	ParticlesSimpleBase pBase;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
